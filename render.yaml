services:
  - type: web
    name: assignment02-isd
    env: docker
    region: singapore # or ohio
    plan: free
    dockerCommand: "uvicorn api.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PORT
        value: 8000
